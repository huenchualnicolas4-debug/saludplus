# Copia este archivo como .env y completa los valores
# IMPORTANTE: nunca subas .env a Git (debe estar en .gitignore)

SECRET_KEY=cambia-esto-por-una-cadena-aleatoria-larga
FLASK_ENV=development

# Para PostgreSQL local (opcional, si no se define usa SQLite):
# DATABASE_URL=postgresql://usuario:password@localhost:5432/saludplus

# En Render.com esta variable se inyecta automáticamente
